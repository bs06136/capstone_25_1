<Window x:Class="overcloud.Views.COP_JoinWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="협업 참가" Height="250" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,20,0,0">
            <TextBlock Text="협업 오버클라우드 이름" Margin="0,0,0,5"/>
            <TextBox x:Name="IdBox" Width="250" Height="30"/>

            <TextBlock Text="접근 코드" Margin="0,10,0,5"/>
            <PasswordBox x:Name="PasswordBox" Width="250" Height="30"/>

            <Button Content="협업 참가 요청" Width="100" Height="30" Margin="0,20,0,0"
                    Click="JoinButton_Click" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
