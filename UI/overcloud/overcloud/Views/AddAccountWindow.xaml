<Window x:Class="overcloud.Views.AddAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:overcloud.Converters"
        Title="계정 추가" Height="280" Width="300" WindowStartupLocation="CenterOwner" 
        Loaded="AddAccountWindow_Loaded">

    <Window.Resources>
        <converters:EmptyStringToVisibilityConverter x:Key="EmptyStringToVisibilityConverter"/>
        <converters:EmptySelectionToVisibilityConverter x:Key="EmptySelectionToVisibilityConverter"/>
    </Window.Resources>


    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- 아이디 -->
            <RowDefinition Height="Auto"/>
            <!-- 비밀번호 -->
            <RowDefinition Height="Auto"/>
            <!-- 클라우드 선택 -->
            <RowDefinition Height="Auto"/>
            <!-- 협업 계정 선택 -->
            <RowDefinition Height="Auto"/>
            <!-- 확인 버튼 -->
        </Grid.RowDefinitions>

        <!-- 아이디 입력 -->
        <Grid Grid.Row="0" Margin="0,10,0,5">
            <TextBox x:Name="txtID" Height="40" />
            <TextBlock Text="아이디 입력" 
                       Foreground="Gray"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       IsHitTestVisible="False"
                       Visibility="{Binding Text, ElementName=txtID, Converter={StaticResource EmptyStringToVisibilityConverter}}"/>
        </Grid>

        <!-- 비밀번호 입력 -->
        <Grid Grid.Row="1" Margin="0,5,0,5">
            <PasswordBox x:Name="txtPassword" Height="40"/>
            <TextBlock Text="비밀번호 입력"
                       Foreground="Gray"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       IsHitTestVisible="False"
                       Visibility="{Binding Password, ElementName=txtPassword, Converter={StaticResource EmptyStringToVisibilityConverter}}"/>
        </Grid>

        <!-- 클라우드 선택 -->
        <ComboBox x:Name="cloudComboBox" Grid.Row="2" Height="40">
            <ComboBoxItem IsSelected="True">
                <TextBlock Text="GoogleDrive" Margin="5,5,0,0"/>
            </ComboBoxItem>
            <ComboBoxItem>
                <TextBlock Text="OneDrive" Margin="5,5,0,0"/>
            </ComboBoxItem>
            <ComboBoxItem>
                <TextBlock Text="Dropbox" Margin="5,5,0,0"/>
            </ComboBoxItem>
        </ComboBox>


        <Grid Grid.Row="3" Margin="0,5,0,5">
            <ComboBox x:Name="cooperationComboBox"
          Height="30"
          Visibility="Collapsed"
          Panel.ZIndex="1"/>
        </Grid>

        <!-- 확인 버튼 -->
        <Button Content="확인"
                Grid.Row="4"
                Width="150" Height="35"
                FontWeight="Bold"
                Background="Black"
                Foreground="White" 
                HorizontalAlignment="Center"
                Margin="0,20,0,0"
                Click="Confirm_Click"/>
    </Grid>
</Window>
